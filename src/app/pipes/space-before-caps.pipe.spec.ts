import { SpaceBeforeCapsPipe } from './space-before-caps.pipe';

describe('SpaceBeforeCapsPipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceBeforeCapsPipe();
    expect(pipe).toBeTruthy();
  });
});
